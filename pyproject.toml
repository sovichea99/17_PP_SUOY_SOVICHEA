[tool.poetry]
name = "rag-baseline-app"
version = "0.1.0"
description = "Baseline Chat-with-Documents RAG application"
authors = ["KSHRD AI Engineering"]
package-mode = false

[tool.poetry.dependencies]
python = "^3.10"
fastapi = "^0.115.0"
uvicorn = { extras = ["standard"], version = "^0.30.0" }
chromadb = "^0.5.0"
ollama = "^0.4.0"
pypdf = "^5.0.0"
tokenizers = "^0.23.2"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
